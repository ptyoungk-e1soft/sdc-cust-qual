# Hugging Face 토큰 (Cosmos Reason 모델 다운로드용)
HF_TOKEN=your_huggingface_token

# 모델 경로 (fine-tuned 모델 사용 시)
MODEL_PATH=nvidia/Cosmos-Reason1-7B
# MODEL_PATH=./output/display_defect/checkpoint-final

# Neo4j 설정
USE_NEO4J=false
NEO4J_PASSWORD=password

# 양자화 (메모리 제한 환경)
QUANTIZE=false
